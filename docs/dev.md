# lasx_rs 开发文档：架构与性能

> 面向**维护者与二次开发者**：内部分层、不可破坏的约定、测试与 CI、性能方法学与实测数据。
> **算子语义与调用方法**（FFI/Rust API/池用法、43 个导出符号）见 [`docs/ops.md`](ops.md)。

本文的实测数据除特别注明外，均为 **2026-09-22** 一次性全量测量：
Loongson-3B6000（LoongArch64/LA664，2.2 GHz，24 逻辑核），负载 `0.54 → 2.66`（测量开始 → 结束），
`rustc 1.100.0-nightly`，numpy 2.5.2（OpenBLAS 0.3.34）。**本机有后台负载，同一读数跨轮次能差 ±20%**，
所以文中给出范围或注明是同一轮 A/B 的对照，不要拿不同时间的绝对值直接比较。

---

## 1. 仓库结构与分层

```text
lasx_rs/
├── src/
│   ├── lib.rs            # crate 根：模块声明 + 历史 API 重导出
│   ├── ops/              # 算子实现，**一个算子一个 .rs**（23 个文件）
│   ├── ffi/              # 符号导出层：裸指针 → 切片 → ops（10 个文件）
│   ├── api.rs            # 安全 Rust API（切片入、Result 出，22 个函数）
│   ├── arch/             # LASX/LSX 样板：类型别名、载入/存储/广播、SimdPath
│   ├── pool.rs           # 常驻工作池 WorkerPool
│   ├── parallel.rs       # 池化的组合算子（matmul 行切、RK4 数组切）
│   ├── aligned.rs        # AlignedVec / AlignedBuf（32 字节对齐）
│   └── scalar_ref.rs     # 标量参考实现（测试与基准对照）
├── cli/                  # 基准与分派检查工具（11 个套件）
├── examples/             # 调用方示例与 A/B 工具（6 个）
├── yll/                  # YouLiLong 脚本语言的原生扩展
└── docs/                 # ops.md（用法）+ dev.md（本文）
```

分层职责与"谁能改什么"：

| 层 | 职责 | 不变量 |
|---|---|---|
| `ops/` | 纯计算，吃 `&[T]` / `&mut [T]`，不碰裸指针 | 每条向量路径必须与标量参考**逐位一致** |
| `ffi/` | `#[unsafe(no_mangle)] extern "C"`，裸指针 → 切片，`_checked` 变体写 `int *status` | **43 个导出符号**中原始 15 个的签名与语义永不改动 |
| `api.rs` | 安全 Rust 接口，做长度/域校验，返回 `Result<_, api::Error>` | 校验不得改变计算结果（位精确同样成立） |
| `pool.rs` + `parallel.rs` | 并行派活，切块必须与串行结果逐位一致 | 切块边界不得改变每个元素的累加次序 |
| `arch/` | `transmute` 与 intrinsic 的唯一集中地 | 内核代码里不出现裸 `m256i` ↔ `m256` 转换 |
| `cli/` / `examples/` | 只测量与演示，不参与生产路径 | 基准不得为"好看"而改口径 |

命名规范（本仓库统一口径）：

- 导出符号：`lasx_<算子>`，校验版 `lasx_<算子>_checked`；新增符号**只能追加**。
- 内部函数：`<算子>`（`ops/` 内 `pub(crate)`），微内核常见后缀 `_lasx` / `_lsx` / `_packed` / `_chunk` / `_cols_block`。
- 常量：路径阈值用 `PACK_MIN_*` / `COL_*` / `K_ALIGN*` / `L1_BUDGET` / `K_CHUNK`，一律带注释说明**实测来源**。
- 测试：`test_<算子>_matches_reference`（数值）、`<路径>_bit_exact`（位精确）、`cols_bit_exact` 之类按路径命名。
- 代码注释与提交信息用中文；提交信息写清"动机 + 实测数据 + 被否掉的选项"。

**零依赖**：`Cargo.toml` 没有任何第三方依赖；稳定工具链不可用（需要 `#![feature(stdarch_loongarch)]`）。

---

## 2. 不可破坏的约定（改代码前必读）

### 2.1 位精确承诺

同一算子的**所有实现路径**（LASX 256 位 / LSX 128 位 / 标量尾 / 打包 / 列块 / 分块）对同一输入必须给出
**逐位相同**的 `f32`/`f64` 结果。测试用 `to_bits()` 比较而不是 `assert_eq!` 浮点值。

推论（这些是"红线"，改动时必须一并检查）：

- 不能为了让向量化更顺而改变浮点结合次序：例如 `norm3` 固定 `x² + (y²+z²)`，`quat_*` 固定标量在前的乘法次序。
- k 方向分块、行分块、列分块**可以**切，但每个输出元素沿 k 的累加次序必须保持递增；f32/f64 部分和落到内存再读回是精确的，所以"分块 + 回写 C"不破坏位精确（见 §9.3）。
- 归约类内核（`sum`、`dot`）的固定分块方式决定了结果，改动分块宽度会改变结果 —— 除非同步改标量参考，否则不允许。
- 换更快的近似（`xvfrecpe` + 牛顿迭代、`xvrsqrt` 等）属于**改数值**，需要明确授权后才可做。

### 2.2 FFI 硬契约

- 最初的 15 个 `lasx_*` 符号（`lasx_alloc`、`lasx_dot`、`lasx_dot_f64`、`lasx_sum`、`lasx_axpy`、`lasx_matmul`、
  `lasx_matmul_f64`、`lasx_dot_i8`、`lasx_dot_q4`、`lasx_ballistic_step`、`lasx_batch_distance2d`、
  `lasx_norm3_batch`、`lasx_vec3_add_scaled_batch`、`lasx_j2_accel_batch`、`lasx_rk4_j2_step_batch`）
  的**签名与语义永不改动**；任何性能改动都必须保证它们的行为不变。
- 新增能力一律"追加新符号"（例如后来加的 7 个姿态/几何批处理算子）或走 Rust API（`api.rs`、`pool`），
  不改老符号。
- 每次改动后校验符号集合：

```bash
cargo build --release
nm -D --defined-only target/release/liblasx_rs.so | awk '$2=="T" && $3 ~ /^lasx_/ {print $3}' | sort
# 当前：43 个（22 个裸版本 + 21 个 _checked），与上一版比较应只有"新增"
```

### 2.3 数值约定（跨算子汇总）

| 约定 | 内容 |
|---|---|
| 四元数 | **标量在前** `(w, x, y, z)`；归一化时 `|q| < 1e-15 → (1,0,0,0)` |
| 零向量 | `unitize3` 对 `|v| = 0` 返回 `(0,0,0)`（不是 NaN） |
| 旋转方向 | `quat_rotate_batch` / `quat_to_dcm_batch` 按 **body → inertial** 的 R |
| `norm3` | `sqrt(x² + (y²+z²))`（这个结合次序是契约） |
| 量化点积 | `dot_i8`：i8×i8 精确 i64；`dot_q4`：每 32 字节一组 scale，组内精确整数点积后乘 scale 积进 f64 |
| 分批求和 | `sum`/`dot` 的向量宽度决定分块，尾块按同样结合次序处理 |
| 溢出 | 所有长度参数按 `usize` 检查；`m*k` 等乘积溢出走 `api::Error::Overflow`，标量参考不参与 |

### 2.4 错误通道

- C ABI：`lasx_*_checked` 在末尾多一个 `int *status`，成功写 0、失败写负值域错误码；裸版本不校验（历史契约）。
- Rust：`api.rs` 的每个函数返回 `Result<_, api::Error>`，`Error` 覆盖 `Shape`（长度不符）、`Overflow`（乘积溢出）、
  `NotFinite`（输入含 NaN/Inf，仅对声明了该检查的算子）、`NotPositive`（如 `sum` 的长度为 0 之类）。
- `parallel.rs` 侧的失败通过 `assert` 上抛（`WorkerPool` 用 `catch_unwind` + `resume_unwind` 保证 panic 不被吞）。

---

## 3. 并行层

`WorkerPool`（`src/pool.rs`）是库内的常驻池，**不是线程池库**：

| 机制 | 说明 |
|---|---|
| 派活 | 原子代次 + 定长作业槽（`MAX_ARRAYS = 16` 个指针 + 长度），派活只做指针算术与几次写入 |
| 唤醒 | worker 先自旋一小会儿再休眠（本机实测这比每次都 futex 唤醒快得多，见 §3.1） |
| 粒度 | 一次调用涉及的元素总数 < `MIN_PARALLEL_LEN = 4096` 时**原地串行**，不派活 |
| 切块 API | `for_each_chunk_mut`（单数组）、`for_each_chunks_mut`（多数组同一范围）、`for_each_row_block_mut`（按行块，用于矩阵乘） |
| 借用 | 派活接口取 `&mut self`，编译期保证不会重入 |
| panic | worker 内 `catch_unwind`，主线程 `resume_unwind`，不吞异常 |

`parallel.rs` 是"组合算子"层：`matmul_f32/f64` 按行块切（块粒度 4 行，与微内核的 4 行对齐）、
`rk4_j2_step_batch` 按 6 个 SOA 数组切。切块只为并行，**不改变每个元素的运算次序**。

### 3.1 池的实测（`lasx_rk4_j2_step_batch`，n = 2^18，原地单步，2026-09-22）

| 线程数 | 每次新建线程 | 库内常驻池 | 池相对 1 线程 | 池效率 |
|---|---|---|---|---|
| 1 | — | 6.98 ms | 1.00× | 100% |
| 2 | 3.59 ms | 3.51 ms | 1.99× | 99% |
| 4 | 1.88 ms | 1.75 ms | 3.98× | 99% |
| 6 | 1.33 ms | 1.22 ms | 5.72× | 95% |
| 8 | 1.08 ms | 894.7 µs | 7.80× | 98% |
| 12 | 979.0 µs | 1.01 ms | 6.92× | 58% |
| 24 | 1.04 ms | 859.6 µs | 8.12× | 34% |

> 24 次线程创建/回收的固定开销实测 ≈ 494.1 µs（只影响"每次新建线程"一列）。
> 12/24 线程的"效率"列低于 100% 是**内存带宽**造成的（本机后台负载下聚合上限约 14–20 GB/s，见 §6.3），
> 不是派活开销——派活开销已经被池压到微秒级。
> 跨步复用的真实收益：65536 星 × 200 步场景里，池建一次跨 200 步复用 → 比每步新建线程快 **1.77×**（见 §11 场景套件）。

---

## 4. 测试与验证

当前规模：**74 个单元测试 + 6 个文档测试**（`cargo test --release`）。

| 测试类型 | 目的 | 例子 |
|---|---|---|
| 参考对照 | 与 `scalar_ref` 的独立实现比数值（带容差） | `test_matmul_f32_matches_reference` |
| **位精确** | 不同路径之间逐位一致（`to_bits`） | `packed_paths_bit_exact`、`cols_bit_exact`、`packed_f64_bit_exact` |
| LSX 对照 | 强制 LSX 路径后与 LASX 结果逐位比对 | `lasx_force_lsx_thread` 相关用例 |
| 边界 | 空、单元素、非 4/8/16/32 倍数、尾部行列 | `test_*_edges` |
| 溢出/错误 | `api::Error::Overflow`、`Shape` 分支 | `api` 层用例 |
| 量化精度 | `dot_i8`/`dot_q4` 对照精确整数参考 | `test_dot_q4_matches_exact_reference` |

**加一个算子的清单**（照做即可保持仓库一致性）：

1. `src/ops/<算子>.rs`：写 LASX 与 LSX（若适用）两条路径 + 标量尾，注释写清数值约定与出处；
2. `src/ffi/<分类>.rs`：加 `#[unsafe(no_mangle)] pub extern "C" fn lasx_<算子>` 与 `_checked` 变体（后者用 `ffi::checked` 的样板）；
3. `src/lib.rs`：重导出（历史 API 兼容层）并在 `api.rs` 加安全版本；
4. 测试至少三条：对照参考、位精确（路径间）、边界；
5. `cli/src/suites/<分类>.rs` 加一行基准（含规模与吞吐单位），`cli/src/suites/mod.rs` 注册套件名；
6. `docs/ops.md` 补语义，`docs/dev.md` 若涉及新阈值/新结论则在 §9/§10 记一条；
7. `cargo fmt --all && cargo clippy --workspace --release --all-targets && cargo test --release`（CI 会再跑一遍，零警告是硬门槛）。

`scalar_ref` 的注意事项：**"+lasx 下 LLVM 会自动向量化标量参考"**，所以基准里的"标量列"常常不是真标量
（例如 `dot` 小 n 时"标量"反而更快，是因为它走了不同的循环结构）。要"真标量"必须用
`lasx_force_lsx_thread`/`-C target-feature=-lasx` 之类的手段隔离，或直接看**锚点占比**（§6.1）。

---

## 5. 构建、工具链与 CI

```bash
cargo build --release                     # 库 + CLI 的默认构建
cargo test --release                      # 74 单测 + 6 文档测试
cargo clippy --workspace --release --all-targets   # 零警告是硬门槛
cargo fmt --all --check
cargo run -p lasx_bench --release -- <套件名子串>   # 基准（不给过滤就跑全部）
cargo run --release --example matmul_ab -- 512 512 512 packed
```

- 工具链：nightly（`#![feature(stdarch_loongarch)]`）。`.cargo/config.toml` 里
  `rustflags = ["-C", "target-feature=+lasx"]`，因此**默认产物就假定 CPU 有 LASX**；
  无 LASX 的机器需要显式去掉该 flag 并接受"LASX-only 内核会执行非法指令"（见 ops.md 的 Caveats）。
- 两个 workflow：`.github/workflows/ci.yml`（GitHub 侧）与 `git-panel-ci.yml`（局域网 panel 侧）。
  panel CI 四步：`Format check` → `Build release` → `Test release` → `Clippy (zero warnings guard)`，
  约 20–25 s；**CI 从不跑基准**（基准受负载影响，不适合当门禁）。
- 提交纪律：中文提交信息；一次改动一个主题；**必须写明受控 A/B 的读数与口径**；被否掉的方案也要记（§10）。

---

## 6. 性能方法学与机器上限

### 6.1 口径：小规模比"相对标量"，大规模比"锚点占比"

- 工作集装得下 L2/L3 时，报 `LASX/标量` 与 `LASX/LSX`（宽度红利是否吃到）。
- 工作集远超 L3 时，"相对标量"失去意义（两者都被带宽压平），改报：

```text
实际 GB/s = 本次调用必须搬运的字节数 / 时间
锚点      = 同规模、同读写比例的"纯搬运"实测 GB/s
占比      = 实际 / 锚点
```

锚点必须**实测且分档**（本机对"单流/双流/6 流/读写混合"都敏感，见 §7.1）。

### 6.2 机器

| 项 | 值 |
|---|---|
| CPU | Loongson-3B6000（LA664 核），2.2 GHz，12 物理核 × 2 SMT = 24 逻辑核 |
| 缓存 | L1d 64 KiB/核，L2 3 MiB/核，L3 32 MiB 共享 |
| 内存 | ~15 GB |
| 向量 | LASX 256 位（8×f32 / 4×f64），LSX 128 位 |

### 6.3 上限：FP 流水线与 A 广播墙（`examples/kernel_probe.rs`，2026-09-22）

| 探针 | 内容 | FMA/周期 | GFLOP/s |
|---|---|---|---|
| `fma16` | 16 条 `xvfmadd.s`，无访存 | **3.96** | **140.3**（f32 吞吐上限） |
| `dfma16` | 16 条 `xvfmadd.d`，无访存 | **3.97** | **69.9**（f64 吞吐上限） |
| `norepl` | 16 FMA + 4 条 B 载入 | 2.66 | 93.8 |
| `kernel` | 16 FMA + 4 条 B 载入 + 4 条 A 标量广播 | **2.00** | 70.3 |
| `dkernel` | 同 `kernel`，f64（4 行 × 16 列） | **1.99** | 35.1 |
| `cRxC` | 扫描 2–8 行 × 16–64 列（累加器 ≤ 16） | **1.98–2.00** | — |

三条结论：

1. **FP 流水线是"4 条 256 位 FMA/周期"，与元素宽度无关**（所以 f64 的 FLOP 上限正好是 f32 的一半）。
2. **只要含 A 标量广播，内核就停在 2.0 FMA/周期**，与行数/列数/累加器个数无关（每行每 k 步恰好 2 周期）。
   k 展开（把 uops/FMA 从 1.94 降到 1.59）实测仍是 1.99 ⇒ **不是发射宽度受限**，微内核没有"再挤一挤"的空间。
3. 因此矩阵乘的**可达天花板**：f32 70.3、f64 35.1 GFLOP/s；当前 512³ 分别到 59.7 与 27.6 GFLOP/s
   （85% / 79%）。想再高只能改算法结构（多累加器、A 打包 + lane 广播），或者接受数值变化。

两个**测量陷阱**（都实际踩过）：

- **链数**：纯 FMA 探针必须 ≥ 16 条独立累加链才量得到吞吐；8 条链时只有 93.6 GFLOP/s
  （≈2.66 条/周期），那是延迟上限（FMA 延迟 ≈3 周期）。旧版报告把 93.2 当"峰值"，已更正
  （`cli/src/suites/micro.rs` 也改成了 16 链）。
- **重复次数**：纯 FMA 一轮只有约 0.7 µs。若沿用访存型内核的"每样品 200 轮"，计时分辨率会把它吃掉，
  看上去像"被优化掉了"（实测打印 0.000 ms）。探针对纯 FMA 用 5×10⁶ 轮、访存型用 200 轮。

### 6.4 A/B 的可信口径

本机有后台负载（同形状跨轮次 ±20%），所以：

| 方法 | 用法 | 陷阱 |
|---|---|---|
| **进程级交替** | 同一形状交替跑 A/B 两个进程，各取中位数 | 最稳；单轮仍可能偏，至少 3 轮 |
| **stash 单文件重建** | 只改一个文件，`cp` 备份 → 构建 → 换回 → 再构建，得到两个二进制再交替 | 适合"两个版本"的对比 |
| 同进程交替 | 同一进程里交替调用两条路径 | **会被彼此的缓存足迹污染**，只在两者足迹相近时可用 |
| 单次跨进程比较 | 直接比两次运行的绝对读数 | 不可用（§6.1 的 ±20%） |

基准里的固定开销也要算清：`SimdPath::detect()` ≈ 0.9 ns/次（线程级 TLS + 原子读），
`lasx_dot` n=24 只要 12 ns，所以**小规模时先看分派开销**（`dispatch` 套件），别把它当成内核慢。

---

## 7. 各算子实测（2026-09-22 全量套件）

复现：`cargo run -p lasx_bench --release`（不给过滤就跑全部），
或 `-- <子串>` 只跑匹配的套件（如 `-- dot_q4`）。计时为"预热后自适应重复到 ~25 ms/样品、5 次取中位数"。

### 7.1 大集锚点（工作集 4–32 MiB，纯搬运）

| 规模 | 单流只读 | 双流只读 | 双流**字节**读 | **6 流 3读3写** | 单线程拷贝 | 12 线程只读 | 12 线程拷贝 |
|---|---|---|---|---|---|---|---|
| 4 Mi | 14.7 | 10.7 | 20.5 | 5.8 | 6.7 | 21.3 | 17.2 |
| 16 Mi | 10.2 | 9.1 | 12.6 | 6.8 | 6.1 | 16.3 | 14.8 |
| 8 Mi (f32) | 11.0 | 8.4 | 19.2 | 5.3 | 6.3 | 17.3 | 15.2 |
| 32 Mi (f32) | 7.5 | 8.9 | 8.2 | 6.8 | 5.8 | 17.4 | 15.0 |

（单位 GB/s。字节读锚点的数组是"每流 n 字节"，f64 锚点是"每流 8n 字节"，同一 n 下 footprint 差 8 倍，
只能比同 footprint 的行。"6 流 3读3写"是 `j2_accel_batch` 这类"3 进 3 出"内核的形态。）

要点：**单线程流式只有 5–15 GB/s，12 线程才 14–21 GB/s**；读写混合比纯读低 30–40%；
6 流与规模无关地压在 5.3–6.8 GB/s。大集下**并行度比指令宽度重要**。

### 7.2 大集下各内核的占比

| 内核 | 实际 GB/s | 锚点 | 占比 | 判定 |
|---|---|---|---|---|
| `rk4_j2_step f64`（单线程） | 3.0–3.1 | 8.1–10.7 | **28–38%** | 算力受限（每元素 1 次 sqrt + 多次除；速率 37 M/s 与规模无关） |
| `dot_q4` | 3.6–7.2 | 8.2–19.2 | **38–44%** | 已到 IPC≈2 的发射上限（§6.3），再快需改浮点结合次序（未授权） |
| `norm3_batch f64` | 7.2–7.3 | 9.2–12.7 | 57–79% | 已接近锚点；4× 展开实测无收益（§10） |
| `j2_accel_batch f64` | 4.2–4.6 | 5.8–6.8 | **62–80%** | 6 流锚点（实测 5.3–6.8）下已到 62–80%，算力受限 |
| `dot_f64` | 9.4–9.6 | 10.2–14.7 | 65–93% | 双流只读，接近上限 |
| `dot_i8` | 4.8–13.4 | 8.2–19.2 | 58–70% | 字节流；4× 展开已落地（+4~16%） |
| `vec3_add_scaled f64` | 8.7–9.4 | 8.8–12.0 | 78–98% | 已在墙上 |
| `dot`/`sum` f32 | 8.1–11.6 | 7.5–11.0 | 94–127% | 已超锚点（读多写少） |
| `axpy` f32 | 10.3–12.6 | 6.9–9.4 | 133–149% | 已超锚点 |
| `rk4_j2_step f64`（12 线程） | 13.6–14.1 | 15.5–19.3 | 71–91% | 接近聚合上限 |

### 7.3 小规模逐内核（相对标量/LSX）

| 内核 | n | LASX | 强制 LSX | 标量 | LASX/标量 | LASX/LSX | 吞吐 |
|---|---|---|---|---|---|---|---|
| `lasx_dot` f32 | 24 | 11 ns | 13 ns | 18 ns | 1.64× | 1.18× | 2.18 G elem/s |
| `lasx_dot` f32 | 64 | 17 ns | 28 ns | 59 ns | 3.47× | 1.65× | 3.76 G elem/s |
| `lasx_dot` f32 | 4096 | 262 ns | 644 ns | 5.6 µs | 21.39× | 2.46× | 15.63 G elem/s |
| `lasx_dot` f32 | 1048576 | 183.4 µs | 292.3 µs | 1.44 ms | 7.83× | 1.59× | 5.72 G elem/s |
| `lasx_sum` f32 | 65536 | 6.1 µs | — | 89.6 µs | 14.79× | — | 10.83 G elem/s |
| `lasx_sum` f32 | 4194304 | 481.4 µs | — | 5.81 ms | 12.08× | — | 8.71 G elem/s |
| `lasx_axpy` f32 | 65536 | 15.3 µs | — | 14.6 µs | 0.96× | — | 8.58 G elem/s |
| `lasx_axpy` f32 | 4194304 | 2.65 ms | — | 2.90 ms | 1.09× | — | 3.16 G elem/s |
| `lasx_dot_f64` | 32768 | 21.8 µs | — | 44.8 µs | 2.05× | — | 1.50 G elem/s |
| `lasx_dot_f64` | 2097152 | 1.85 ms | — | 5.15 ms | 2.78× | — | 1.13 G elem/s |
| `lasx_dot_i8` | 65536 | 5.6 µs | — | 10.2 µs | 1.81× | — | 11.60 G elem/s |
| `lasx_dot_i8` | 4194304 | 372.1 µs | — | 731.3 µs | 1.97× | — | 11.27 G elem/s |
| `lasx_dot_i8` | 33554432 | 12.90 ms | — | 15.17 ms | 1.18× | — | 2.60 G elem/s |
| `lasx_dot_q4` | 65536 | 11.1 µs | — | 140.1 µs | 12.63× | — | 11.82 G nibble/s |
| `lasx_dot_q4` | 4194304 | 1.02 ms | — | 14.72 ms | 14.42× | — | 8.22 G nibble/s |
| `lasx_dot_q4` | 33554432 | 17.78 ms | — | 79.60 ms | 4.48× | — | 3.77 G nibble/s |

注意：`axpy`/`sum` 的"标量列"被 LLVM 自动向量化了（§4），所以 `axpy` 只有 0.96×；
`dot` 小 n（8/16）走标量快路径（无能力探测），此时 LASX 反而更慢（0.60×）——这是**有意的**
（小 n 下分派与向量建立开销占主导）。

### 7.4 批量物理/姿态内核

| 内核 | n | LASX | 标量 | 比 | 吞吐 |
|---|---|---|---|---|---|
| `lasx_norm3_batch` f64 | 65536 | 128.2 µs | 127.1 µs | 0.99× | 511.1 M sample/s |
| `lasx_norm3_batch` f64 | 1048576 | 2.69 ms | 2.66 ms | 0.99× | 390.2 M sample/s |
| `lasx_vec3_add_scaled_batch` f64 | 1048576 | 10.56 ms | 10.53 ms | 1.00× | 595.6 M elem/s |
| `lasx_batch_distance2d` f32 | 1048576 | 1.11 ms | 1.11 ms | 1.00× | 940.7 M point/s |
| `lasx_j2_accel_batch` f64 | 65536 | 366.5 µs | 366.6 µs | 1.00× | 178.8 M sample/s |
| `lasx_j2_accel_batch` f64 | 1048576 | 9.09 ms | 8.71 ms | 0.96× | 115.4 M sample/s |
| `lasx_ballistic_step` f32 | 1048576 | 2.58 ms | 3.05 ms | 1.18× | 405.9 M shot/s |
| `lasx_rk4_j2_step_batch` f64 | 262144 | 7.04 ms | 7.16 ms | 1.02× | 37.2 M sample/s |
| `lasx_cross3_batch` f64 | 4096 / 262144 | 6.3 µs / 821 µs | 40.6 µs / 3.28 ms | 6.42× / 3.99× | 2.87–5.83 G flop/s |
| `lasx_unitize3_batch` f64 | 4096 / 262144 | 20.1 µs / 1.29 ms | 60.0 µs / 4.02 ms | 2.99× / 3.11× | 2.23–2.24 G flop/s |
| `lasx_quat_rotate_batch` f64 | 4096 / 262144 | 52.6 µs / 4.10 ms | 162.7 µs / 11.23 ms | 3.10× / 2.74× | 2.56–3.12 G flop/s |

- `norm3`/`vec3_add_scaled`/`distance2d`/`j2` 的"标量列"是 1.00×：这些内核的标量参考在 `+lasx` 下
  被 LLVM 完整向量化，所以**这四行的"标量"其实是另一份向量实现**；它们的收益要看大集占比（§7.2）。
- 姿态批处理（`cross3`/`unitize3`/`quat_rotate` 及另外 4 个未进 CLI 套件的 `mat3_mul_vec3`/`quat_normalize`/
  `quat_mul`/`quat_to_dcm`）在"标量真的没被向量化"的口径下是 **2.7–6.4×**（见 §10 的历史记录）。
  CLI 的 `attitude` 套件目前只样板了 3 个；补全 4 个是已知缺口（§12）。

### 7.5 对齐与分派开销

| 项 | 结果 |
|---|---|
| 32/64 字节对齐（`lasx_dot` n=8192） | 605 ns（对齐）vs 993 ns（偏移 1 元素）= **1.64×** |
| `lasx_dot` n=65536 | 22.5 µs（对齐）vs 20.1 µs（未对齐）= 0.89×（大集下带宽主导，对齐不再重要） |
| `lasx_dot_f64` | 对齐/未对齐 1.00×（f64 内核用 `xvld`，对 32 字节对齐不敏感） |
| `SimdPath::detect()` | 0.91 ns/次（≈2 倍空循环 0.46 ns） |
| `hardware()`（纯原子读） | 0.91 ns/次 |

### 7.6 真实调用形态

**多星多步轨道传播**（n = 65536 星 × 200 步，dt = 10 s）：

| 推进方式 | 200 步总时间 | 吞吐 | 相对单线程 |
|---|---|---|---|
| 单线程 | 350.59 ms | 37.4 M sample·step/s | 1.00× |
| 12 线程 · 每步新建线程 | 88.78 ms | 147.6 M | 3.95× |
| 12 线程 · 库内常驻池（跨步复用） | **50.20 ms** | 261.1 M | **6.98×** |

**融合内核 vs 拼原语**（RK4 步，n = 65536，20 步）：

| 调用策略 | FFI 调用/步 | 20 步总时间 | 吞吐 |
|---|---|---|---|
| `lasx_rk4_j2_step_batch`（融合） | **1** | **35.60 ms** | 36.8 M sample·step/s |
| 拼 18 次原语（`j2_accel` + `vec3_add_scaled`） | 18 | 99.06 ms | 13.2 M |

> 拼接版慢 2.8×：除了 18 次调用开销，每次调用都要把 6–9 个 f64 数组写回内存再读出来，
> 而这些中间量在融合版里从未离开寄存器。**"融合"是本库最有效的调用侧优化**。

**大矩阵乘铺到多核**（`parallel::matmul_f32`，12 线程）：

| 规模 | 单线程 | 12 线程 | 加速比 | 多核 GFLOP/s |
|---|---|---|---|---|
| 64³ | 7.7 µs | 1.9 µs | 4.10× | 280.7 |
| 128³ | 63.6 µs | 7.4 µs | **8.54×** | **563.1** |
| 256³ | 556.5 µs | 102.5 µs | 5.43× | 327.2 |
| 512³ | 4.59 ms | 1.04 ms | 4.41× | 257.7 |

> 加速比到不了 12× 是 **B 的流量压共享缓存**（每个 4 行块都要重读整个 B），不是派活。
> 线程数也不是越多越好：`examples/matmul_pooled.rs` 扫描显示 16 线程 ≈ 8.5×、24 线程反而退化。

---

## 8. 矩阵乘深挖

### 8.1 三条路径与分派

`matmul_f32` 有三条**逐位一致**的路径（数字阈值都在 `src/ops/matmul.rs` 顶部、带实测注释）：

```text
1) 打包（packed）   m ≥ 32 且 k ≥ 64 且 m·k·n ≥ 8M   例外：m ≤ 128 && n ≥ 1024 && n > 16384 && k < 192 时不用
2) 列块在外（cols） m ≤ 128 且 n ≥ 1024
3) 流式（stream）   其余
```

f64 只有两条（打包 / 流式），`m ≥ 32 && k ≥ 64 && m·k·n ≥ 8M` 走打包。

**小 n 的负优化**是有意的：`dot`/`matmul` 在 n 很小时分派与向量建立的开销大于收益。

### 8.2 打包（packing）

把 B 的 32 列（f64 是 16 列）打成 `packed[(strip*k + p)*32 + r]` 的连续条带，微内核的 k 循环
（`tile4x32_packed` / `tile4x16_chunk`）就变成顺序读；否则每一步都要按 `n` 跨距访问 B。

打包还有第二个收益（§9.4）：**消除跨距别名**。当 `n` 是 2 的幂、B 的相邻行正好相距 L1 容量
（n = 16384 时为 64 KiB）时，列块路径的所有行映射到同一批组，带宽掉一半以上；打包后的连续条带
完全不受影响。

### 8.3 k 分块（k-chunking）

打包条带按 k 主序存放，所以"一段 k"就是一段连续内存。把 k 切成 `K_CHUNK`（f32/f64 均为 256 步，
= 32 KB 条带），循环次序改成 **k 块在外、行块在内**：

```text
for 列条带（32 列）:
  for k 块:                 # 条带 ≤ 48 KB 时自适应地不分块
    for 行块（4 行）:        # 该 k 块的 B 被所有行块反复命中，留在 L1
      16 个累加器 × kb 步 FMA
```

- 动机：条带 > L1（64 KiB）后每步都要回 L2/L3 取 B。`512×k×512` 的条带扫描：24–48 KB 时 59–61 GFLOP/s，
  正好等于 L1 的 64 KB 掉到 54.7，96–128 KB 只有 47–49。
- 代价：C 要按 k 块读写（k=1024 时多 3 次），相对算力可忽略。
- **逐位不变**：块间把 C 里的部分和读回来继续按 p 递增累加，f32/f64 部分和落盘/读回是精确的。

### 8.4 阈值重标定（k 下限 192 → 64）

§19 时代的 `k ≥ 192` 是**打包前**的结论；k 分块改变了成本结构后必须重测：

| 形状 | 旧策略 | 新策略 | 变化 |
|---|---|---|---|
| 256×64×4096 | 22.1 | **58.7–61.6** | +165% |
| 64×128×4096 | 22.4 | **47.2–50.9** | +115% |
| 64×96×8192 | 22.0 | **46.3–51.3** | +110% |
| 128×128×2048 | 31.4 | **56.9–57.5** | +80% |
| f64 256×64×4096 | 11.2 | **28.6–30.2** | +160% |
| 128×128×128 / 96×128×512 | 67.5 / 59.3 | 67.4 / 61.1 | 持平（`work ≥ 8M` 挡住 128³） |
| 100×64×19147 | **54.5–61.8**（列块） | 33.5–41.9（打包） | 例外：小 k + 极宽 n 保留列块 |

### 8.5 当前性能与参照

| 形状 | 本库（单线程） | OpenBLAS 0.3.34（单线程，同时段） | 差 |
|---|---|---|---|
| f32 64³ | **68.5** | 28.9 | 我们快 2.37× |
| f32 128³ | **66.7** | 49.8 | 快 1.34× |
| f32 192³ | — | 55.2 | — |
| f32 256³ | **61.8** | 59.2 | 快 1.04× |
| f32 384³ | **60.0** | 61.9 | 落后 1.03× |
| f32 512³ | **59.7** | 63.2 | 落后 1.06× |
| f32 1024³ | **50.2** | 59.5 | 落后 1.19× |
| f32 2048³ | **55.0** | 59.8 | 落后 1.09× |
| f32 100×448×19147 | 41.6–44.1 | 48.6 | 落后 1.10–1.17× |
| f32 512×2048×512 | 41.7–48.9 | 62.8 | 落后 1.28–1.51× |
| f64 128³ | **31.3** | 26.0 | 快 1.20× |
| f64 256³ | 28.7 | 30.6 | 落后 1.07× |
| f64 512³ | 27.6 | 31.0 | 落后 1.12× |
| f64 1024³ | — | 29.3 | — |

（单位 GFLOP/s。OpenBLAS 多线程非常强：f32 512³/1024³/2048³ 在 12 线程下是
305.8 / 337.3 / 331.9 GFLOP/s，而本库 `parallel::matmul_f32` 12 线程是 257.7（512³）——
多核大矩阵仍落后，原因是 B 的流量而非派活，见 §7.6。）

相对 §6.3 的内核天花板：f32 512³ 59.7/70.3 = **85%**，f64 512³ 27.6/35.1 = **79%**。

---

## 9. 大集与形状专项

### 9.1 判据：算力受限还是带宽受限

看"每元素速率是否随规模变化"：

| 内核 | 2^14 | 2^18 | 2^22 | 2^24 | 判定 |
|---|---|---|---|---|---|
| `rk4_j2_step`（元素/秒） | 37.2 M | 35.3 M | ~31 M | ~31 M | **与规模无关 ⇒ 算力受限**（每元素 1 次 sqrt + 多次除） |
| `j2_accel_batch`（元素/秒） | — | — | 88 M | 83 M | L2 内 178 M/s、离开 L2 掉一半后缓降 ⇒ 访存 + 每元素除法混合 |

对算力受限的内核，"带宽占比低"不是缺陷，而是算力的直接后果（3.0 GB/s ÷ 96 B/元素 = 31 M/s ✓）。

`j2_accel_batch` 曾长期被怀疑"卡在 6 流上限"，补测 6 流锚点（3 读 3 写、`as_chunks::<4>()` 32 字节块搬运）后
得到 **5.3–6.8 GB/s 且与规模无关**，而它在 4.2–4.6 GB/s ⇒ **62–80%**，不是多流上限，是每元素除法 + sqrt。
（第一版 6 流锚点写成标量循环只有 3.2 GB/s，那是"地板"不是"上限"，已废弃——锚点务必确认是向量化的。）

### 9.2 `100×448 × 448×19147` 这个形状

性质：m 小（100 = 25 个 4 行块）、n 极大（19147）、k 中等（448）。

- 历史：最初 165.9 ms（流式，B 被读 25 遍且跨距访问）→ 列块在外（128 列一块）→ 42.7 ms（**3.9×**）；
  打包 + k 分块后 41.2–44.1 ms（41.6–44.1 GFLOP/s）。
- 踩过的坑：**列区间必须 32 对齐**（否则命中非 FMA 的标量尾，差 1 ulp，位精确测试抓到）。
- 试过但否掉的：**多核按列切**（每个线程各自读一遍 B，缓存互相踩，净亏）。
- 现状：OpenBLAS 48.6 GFLOP/s，本库 41.6–44.1（落后 1.10–1.17×）。剩下的手段是 A 侧打包
  与 C 分块（§12）。

### 9.3 k 分块的位精确性依据

`C` 的部分和以 `f32`/`f64` 存在内存里，写回再读入是**精确**的（同一个二进制浮点值），
所以"分块 → 回写 C → 下一块读回继续加"与"一直在寄存器里加完"给出逐位相同的结果，
只要每块内部沿 k 的次序递增、块之间也递增。这也是 `packed_paths_bit_exact` / `packed_f64_bit_exact`
能过的原因。

### 9.4 跨距别名（2 的幂 n）

`64×128×n` 扫描时，列块路径在 `n = 16384`（B 相邻行相距正好 64 KiB = L1）只有 20.9–22.0 GFLOP/s，
而 `n = 19147`（非 2 的幂）有 48.6–52.1：

| n | 4096 | 8192 | 12288 | 16384 | 19147 |
|---|---|---|---|---|---|
| 列块路径 | 22.7 | 21.7 | 22.1 | 20.9 | **48.6–52.1** |
| 打包路径 | 51.9 | 48.0 | 41.0 | 40.3 | 37.9–40.5 |

结论：**2 的幂的 `n` 会惩罚跨距访问**（冲突缺失），打包路径免疫。这也是把 `PACK_MIN_K`
下调到 64 之后 2 的幂形状收益特别大的原因。

---

## 10. 被否掉的尝试（负面结果清单）

> 这一节比"成功记录"更值钱：这些都是**实测否掉**的，重做之前先看这里。

### 10.1 矩阵乘

| 尝试 | 结果 | 原因 |
|---|---|---|
| 两级 L2 分块 + 更深微块（3/6 行） | 否 | 16 个累加器约束下 4 行 × 32 列已经最优；形状扫描（2–8 行 × 16–64 列）全部落在 2.00 FMA/周期 |
| k 展开 ×2（uops/FMA 1.94 → 1.59） | 否（±1%） | 内层不是发射宽度受限（IPC 3.72 但墙在 2.0） |
| A 广播改"一次载入 + 4 次 `xvshuf4i.w`" | 否（1.92 vs 2.00） | shuffle 同样卡在 2 周期/行 |
| 多核按**列**切（宽形状） | 否 | 每个线程各读一遍 B，缓存互踩 |
| 只看"指令数"就下结论 | 教训 | §6.3：31 uops/16 FMA 但 IPC 3.72，说明瓶颈不在发射 |

### 10.2 `dot_q4`（Q4 量化点积）

| 版本 | 做法 | 结果 |
|---|---|---|
| v3 | 一批 4 组（128 B，8 个载入先发） | **保留**：L2/L3 +7~25%，DRAM 持平 |
| v4 | 组内归约用 4 次 `xvpickve2gr_d` 取 lane，去掉 `xvst` + 标量载入 | 否：2^25 中位 21.40 vs 20.0 ms（慢 7%） |
| v5 | 一批 8 组（256 B，16 个载入） | 否：2^25 中位 19.96 vs 14.87 ms（慢 34%），寄存器压力吃掉 OoO 空间 |

**"组内归约无法省掉"是硬约束**：每 32 字节有自己的 scale，必须"精确整数点积 → 乘 scale → 进 f64"，
拆到 lane 上并行会改变浮点结合次序（未授权）。

### 10.3 其他

| 尝试 | 结果 |
|---|---|
| `norm3_batch` 4× 展开（12 载入先发 + 4 条独立 `xvfsqrt_d`） | 否：各档都在噪声内（CLI 2^20 新中位还偏差），因为它本来就在锚点的 57–79% |
| `j2_accel_batch` 展开 | 否（历史 §11.7 同结论）：已在每元素除法 + sqrt 的算力上限 |
| 用 `xvfrecpe`+牛顿迭代替换 rk4 的除法 | **未授权**（改数值） |
| 把 `dot_q4` 的组内归约拆到 lane 上 | **未授权**（改浮点结合次序） |
| 无损压缩换带宽（`docs/compression.md` 的路线） | 结论在 ops.md：数据本身已近随机、无损压缩压不动，只有"换成更窄的表示"（量化）才有带宽收益 |
| 标量 6 流锚点 | 无效锚点（3.2 GB/s 是地板）；改成 32 字节块搬运后 5.3–6.8 GB/s |

### 10.4 方法学教训（反复踩到）

1. **跨进程单次比较不可用**（±20%）：必须进程级交替或 stash 重建（§6.4）。
2. **套件内相邻内核会污染缓存**：`large` 套件里 `dot_i8`/`dot_q4` 的读数偏低（前面的 f64 内核把缓存洗了），
   量化内核要用单套件口径（`-- dot_q4`）。
3. **锚点必须与内核同 footprint**：字节内核（`dot_i8`/`dot_q4`）要用"双流**字节**读"锚点，
   不能拿 8 倍 footprint 的 f64 锚点比（早期版本算错过占比）。
4. **"标量列"不等于真标量**（§4/§7.4）。
5. **纯 FMA 探针的两个陷阱**（链数、重复次数，§6.3）。

---

## 11. 复现

```bash
# 全量基准（约 1 分钟；输出即本文 §7 的表格）
cargo run -p lasx_bench --release
cargo run -p lasx_bench --release -- large        # 只跑大集分析
cargo run -p lasx_bench --release -- dot_q4       # 单套件口径（量化内核请用这个）
cargo run -p lasx_bench --release -- attitude
cargo run -p lasx_bench --release -- matmul       # f32/f64 方阵 + 宽形状

# 单形状 A/B（进程级交替）：<m> <k> <n> <packed|cols|stream|packed64|stream64>
cargo run --release --example matmul_ab -- 512 512 512 packed
cargo run --release --example matmul_ab -- 100 448 19147 packed

# 微内核探针：<变体> [k] [轮数]，变体见 docs/dev.md §6.3
cargo run --release --example kernel_probe -- kernel 256 10
cargo run --release --example kernel_probe -- dfma16

# 池化矩阵乘 + 线程数扫描
cargo run --release --example matmul_pooled
cargo run --release --example pool_axpy

# OpenBLAS 对照（本机 numpy 即 OpenBLAS 0.3.34）
OPENBLAS_NUM_THREADS=1 python3 -c "
import numpy as np, time
s=512; a=np.random.rand(s,s).astype(np.float32); b=np.random.rand(s,s).astype(np.float32)
a@b; ts=[]
for _ in range(5):
    t=time.perf_counter(); c=a@b; ts.append(time.perf_counter()-t)
t=sorted(ts)[2]; print(f'{s}^3 {t*1e3:.2f} ms {2*s**3/t/1e9:.1f} GF/s')"
```

**做一个受控 A/B 的标准流程**（本仓库全程用这个）：

```bash
cp src/ops/<算子>.rs /tmp/<算子>.bak          # 1) 备份旧版
# 2) 改代码 → cargo build --release → 复制产物为 A
cp target/release/examples/<工具> /tmp/ab_a
cp /tmp/<算子>.bak src/ops/<算子>.rs           # 3) 还原旧版 → 重新构建 → 复制产物为 B
cargo build --release --example <工具> && cp target/release/examples/<工具> /tmp/ab_b
# 4) 交替跑 A/B 各 3 轮取中位数；同轮内比较，不做跨轮绝对值比较
```

新增基准套件：在 `cli/src/suites/<分类>.rs` 里产出 `Row`，再到 `cli/src/suites/mod.rs` 注册名字，
`lasx_bench <子串>` 就能过滤到。套件只写"怎么测"，不写结论。

---

## 12. 已知缺口与待办

| 缺口 | 现状 | 下一步 |
|---|---|---|
| `attitude` 套件只覆盖 3/7 个姿态内核 | `mat3_mul_vec3`/`quat_normalize`/`quat_mul`/`quat_to_dcm` 有实现、有测试，但没进 CLI 基准 | 补上标量参考与 4 行基准 |
| f64 大矩阵 | CLI `matmul` 只到 512³；1024³ 只有历史读数 | 扩规模，并核对是否仍落后 OpenBLAS 1.1× |
| 多核大矩阵乘 | 512³ 12 线程 257.7 vs OpenBLAS 305.8–337.3 GFLOP/s | 减少 B 的重复读（B 打包后跨线程共享？或按列块并行） |
| `f64` 内核离天花板还有 20% | 512³ 27.6 / 35.1 = 79%（f32 是 85%） | 2 行 × 32 列（16 个累加器）或 A 打包 + lane 广播；注意 128³ 由 `work` 阈值保护 |
| 宽形状 `100×448×19147` | 41.6–44.1 GFLOP/s（OpenBLAS 48.6） | A 打包、C 分块、按 n 的 panel 调度 |
| `dot_q4` 大集 | 3.6–7.2 GB/s（38–44%） | 已到 2.0 FMA/周期墙；除非授权改浮点结合次序 |
| `rk4`/`j2` | 算力受限（除法 + sqrt） | 需要授权才能改公式/近似 |
| 无损压缩路线 | 结论已记在 ops.md | 若要做，是"换更窄的表示"而非通用压缩 |
