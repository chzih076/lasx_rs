# 项目规则（维护者与智能体都适用）

> 这份文件是**流程约定**，不是设计文档。设计在 `docs/dev.md`，用法在 `docs/ops.md`。
> 规则存在的理由写在每条后面——不知道"为什么"的规则，早晚会被当成形式绕过去。

## 1. 改动代码只用标准文件工具（read / write / edit）

**禁止**用 shell 文本处理改仓库里的文件：`cat >`、`cat >>`、`sed -i`、`python3 - <<EOF`、
`tee`、`perl -pi` 都不行。多站点机械修改就拆成一串 `edit`，慢一点也拆。

**为什么**：这不是"内容对就行"。`docs/dev.md` 是**决策记录**，它的价值在于每一行都能追溯到
一次标准工具操作——`git diff` 里能看清改了哪几行、和哪些决定绑在一起。用 `cat >>` 追加的内容
在 diff 里是一大坨无法归因的文本，将来回溯"这条决策谁定的、什么时候定的、为什么"时链就断了。
本仓库整套设计哲学是"编译期保证 + 可验证流程"（位精确承诺、CI 零告警、实测 A/B 记录），
流程这一环不能自相矛盾。

**例外**：只读命令（`grep`/`sed -n` 查看、`cargo` 构建与测试、`git` 查询/推送）不受限；
`/tmp` 下的临时验证文件不受限（不进仓库、不进审计链）。

## 2. 提交前看**全量** `git status`，不要只 `git add` 记得改过的文件

**为什么**：真实事故——池的闭包签名从 2 参改成 3 参，却漏 stage 了 `src/parallel.rs`，
推上去的 commit 里"新池 + 旧调用方"对不上，CI 的 build 步骤直接红。本地能过是因为工作区
里那个文件已经改好了。**"我记得改过哪些文件"是最不可靠的记忆**。

## 3. 接口一旦"冻结"，改它要先在 `docs/dev.md` 记一笔

冻结的接口清单在 `docs/dev.md §19.7`（池的行块闭包、`shape::Prepared` 的 `apply*`、
`Policy::threads`）。要改：

1. 先在 `docs/dev.md` 的冻结小节里写"为什么冻结之后还要改"（触发条件、代价、影响面）；
2. 再改代码；
3. 如果这次改动会让已生成的公式 DSL 代码失效，同一提交里改掉。

**为什么**：冻结不是形式，是**成本声明**——第 3 步的 `lasx_rs_macros` 会直接生成调用这些
接口的代码，冻结之后每改一次都要连带改宏与它的测试。"先记录再改"让这个成本可见。

## 4. 数值与性能断言必须带实测

- 位精确是硬承诺（`docs/ops.md §2.5`）：新路径要么与参考**逐位**一致，要么明确写进例外清单。
- 性能结论必须来自**进程级交替 A/B、≥3 轮、丢弃首轮、取中位数**（`docs/dev.md §6.4`）；
  单次取样在本机会骗人到 4.4×。
- "理论上应该一致/更快"不算验收：拿数值或者拿测量。

## 5. 报错与契约要写清"谁守"

写进文档的每条契约都要标明**由什么保证**：类型系统 / 借用检查器 / `debug_assert` /
测试 / 只靠约定。**"只靠约定"的必须点名**，并且指出违反的后果（错结果还是 UB）。

**为什么**：`docs/dev.md §19.7` 那三条契约就是例子——第 1、3 条编译器能守，第 2 条
（闭包捕获的只读数组只能用 `[start, start+rows)`）**只有约定**，写错是静默错结果。
把"靠约定"的那条藏在一堆"编译器保证"里，等于给自己下套。
